import React from 'react';
import ImageContainer from './ImageContainer';

const HeroImage = () => {
  return (
    <div className='hero-image min-h-full '>
      <ImageContainer></ImageContainer>
    </div>
  );
};

export default HeroImage;
